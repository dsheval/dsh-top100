# dsh-top100 本地开发交接

更新时间：2026-09-03

## 1. 交接目标

当前实际仓库为 `/Users/cdy/Desktop/Work/CP/dsh-top100/repo`。界面、官网安装指南及 GitHub/npm README 这一轮调整已收尾，**1.3.0 本地候选已准备，可执行检查已通过**；Docker 构建及容器验收仍受环境阻塞，不代表已完成全部发布验收，不再主动重做界面。详细范围、证据和后续门槛见 [1.3.0 发布验收记录](./release-1.3.0.md)。

发布准备任务已在当前 Local 目录接续，沿用 `feat/discovery-quality-search`。真实 DSH 0.1.1-rc.2 的隔离 CLI 安装及 Web 设置页验收已通过，用户已授权本次 commit/push/PR，目标分支为 `main`，执行结果以 Git 与对应 PR 为准；未授权合并、tag、npm 发布或服务器部署。本机 Docker 不是 npm 发布的必需条件，可由 PR 的 CI 构建镜像，容器运行验收后续在测试/部署环境补齐。服务器、域名、Caddy、SSL 和线上数据库操作继续在独立部署阶段明确目标、备份与授权后处理。

当前前端已经达到可继续迭代的状态。开始新任务时必须保留现有工作树，禁止使用 `git reset --hard`、`git checkout -- web/public/index.html`、`git clean` 或其他会覆盖未提交文件的命令。

## 2. 新任务开始方式

建议新对话直接使用同一项目的 Local 工作目录，沿用 `feat/discovery-quality-search`，避免丢下尚未提交的代码、测试与截图。不要从干净的远端 `main` 重新开始，也不要让旧对话与发布对话同时修改同一目录。先执行只读检查：

```bash
pwd
git status --short
git log -3 --oneline --decorate
```

期望工作目录：

```text
/Users/cdy/Desktop/Work/CP/dsh-top100/repo
```

可直接把下面这段作为新任务的第一条消息：

> 请在 `/Users/cdy/Desktop/Work/CP/dsh-top100/repo` 继续，先阅读 `docs/HANDOFF.md`，沿用 `feat/discovery-quality-search` 并保留全部未提交改动。开始 dsh-top100 1.3.0 发版准备：整理本轮发布范围，同步版本号、锁文件、CHANGELOG 和官网展示，完成全量检查、插件构建及发布包验证，报告结果与剩余风险。不要再做无关的界面调整。提交、push、PR/merge、npm 发布及服务器部署须在对应阶段获得明确授权后执行。

## 3. 当前 Git 状态

正式稳定基线以远端 `main` 与正式标签为准。2026-09-03 本地验收阶段只读核查时，npm `latest` 仍为 `1.2.1`，远端 `main` 对应 `v1.2.1`。本地根包、插件、锁文件和官网版本展示已同步为 `1.3.0`，CHANGELOG 标为“待发布”；Collector/schema 保持独立版本。当前已进入用户授权的 commit/push/PR 阶段；新任务应重新核查分支与 PR 状态，不能把这里的记录当作实时状态。远端为：

```text
origin  https://github.com/dsheval/dsh-top100.git
```

维护时以远端 `main` 和正式标签为准，不在交接文档中记录容易过期的提交哈希或工作树清单。开始修改前先执行 `git status --short` 和 `git log -3 --oneline --decorate`；已有改动默认属于正在进行的工作，除非用户明确要求，不要删除、回退或批量格式化。

### 1.3.0 发布检查清单

本轮状态：类型检查及 374 项测试在本地 Node 26 与隔离 Node 24 环境全部通过；插件构建、真实 prepack/打包、隔离 npm 安装、主客端模块与 Skill/工具注册、快照读取、`git diff --check` 均通过。隔离运行时发布导入 3,414 条样本，生成 3,322 个 Plugin 和 92 个 Skill，118 个快照引用的字节数及 SHA-256 核对通过。Docker Compose 配置检查通过，但 Docker daemon 缺失，镜像构建和容器验收未通过验证。npm README 的三张 `main` 图片链接均为 HTTP 404，正式发布前必须先合入并复核。下面的完整清单继续作为下一阶段门槛，具体限制见验收记录。

宿主补充验收：npm 发布的 DSH 0.1.1-rc.2 使用官方 plugin add 命令安装候选 `.tgz` 成功；真实 Web 设置页中的市场、中文全库搜索、分类保留搜索词、安装源组合筛选、Skills 目录与已安装 1.3.0 均正常，诊断错误/警告/冲突/依赖问题均为 0。本轮仅用临时 profile、本地快照和自动分配回环端口，没有配置模型密钥或安装榜单项目，验收实例已停止；原有预览服务不变。

1. 检查已修改及未跟踪文件，确认本轮范围：官网和插件 UI、全库搜索、安装源解析、Collector 中文简介与分类、README 和三张截图。不得把无关本地文件、凭证或运行时数据一起提交。
2. 同步发布版本和锁文件，补全 CHANGELOG；历史版本记录、兼容接口说明不要机械替换。检查官网展示与安装说明保持一致。
3. 执行 `npm run check`、`npm run plugin:build`、`git diff --check`，再检查生成产物和 npm 包内容。最近一次只跑了 55 项网页测试与跳过构建脚本的 dry-run，不能据此宣称当前候选版本通过完整发版验收。
4. 核对 `.github/workflows/ci.yml`：CI 包含类型检查、全量测试、运行时数据发布和 Docker 镜像构建；当前未自动覆盖 npm 正式发布。条件允许时验证隔离环境中的实际包安装，不能改动用户现用 DSH 配置来充当测试。
5. 经授权后再 commit / push / PR，CI 和审查通过后 merge；从合并后的确定版本生成发布包。npm README 使用 GitHub `main` 的绝对截图链接，发布前确认三张图片均可公开访问。
6. 经授权发布 npm 新版本后，核对包版本、README、图片和安装结果。服务器部署另行确认目标、备份、健康检查及回滚版本，更新 `web` 和 `scheduler`，不是只上传网页。
7. Collector 的安装解析版本已更新为 2，旧检测缓存会在后续正常采集中重建，可能增加该轮请求量；新版简介和安装源证据要等新采集及快照发布才完整生效。不要擅自清库、删除 `runtime/`、强制全量采集或回填线上数据。

## 4. 当前可用功能

### 首页与视觉

- 产品名为 `dsh-top100`。
- 首页使用白色与中性鼠尾草绿分层，深绿承担交互状态。
- Hero 区包含全库搜索、插件榜单与安装入口；右侧恢复发布信息卡片，展示 npm 版本、DSH Web 兼容版本、来源与复制命令。星环暂时移除，后续再单独决定是否以及如何融入。
- `安装指南` 切换到单列操作页：版本前提 → 安装插件 → 启动 DSH Web → 设置中的「插件排行」。只在该视图隐藏首页 Hero，点击品牌标志返回首页。
- `DSHeval` 按钮直接打开 `https://www.dsheval.ai/dsheval`。
- `GitHub` 直达 `https://github.com/dsheval/dsh-top100`。
- 榜单滚动进入视口时按从上到下的顺序显示动画。

### Docs 同页切换

- 点击首页 `Docs` 不打开新页面，而是在榜单区域原位切换为文档内容。
- Docs 状态 URL 为 `/?page=docs#docs`，返回榜单后为 `/#ranking`。
- Docs 内有“返回榜单”按钮，浏览器前进、后退和刷新可以恢复当前视图。
- `web/public/docs.html` 是文档内容源；`index.html` 读取其中的 `.docs-layout` 并嵌入首页，因此不要复制出第二套文档正文。
- 独立访问 `/docs.html` 仍可作为直接文档地址和同页加载失败时的内容源。
- `web/public/dsh.html` 和 `web/public/dsheval.html` 是同页加载的内容片段；前者为接入说明，后者用于承接后续 DSHeval 页面材料。
- DSH 插件随包提供 `recommend-dsh-plugins` Skill，并注册 `dsh_top100_search` 只读模型工具；对话中的插件推荐直接查询同一份实时榜单和共享智能搜索逻辑。
- DSH 插件设置页增加「插件市场 / 已安装 / 诊断」三层导航；分类仍统一使用 Agent增强、外观、编程、知识、工具、安全，管理操作通过同源 Host API 和 profile 串行队列执行。

### 榜单

- `Top 100`：按综合热度评分排序，但列表展示真实 GitHub Stars。
- `新锐榜`：按今日新增 Stars 排序。
- `总榜`：展示全部已验证仓库，按 Stars 总数排序。
- 官网与 DSH 设置页都支持搜索、隐藏 Skill 仓库和分页加载；隐藏后分类榜、总榜及分类按钮数量同步扣除 Skill 仓库，并显示本次隐藏数量。
- 仓库名称本身不可点击；GitHub 圆形按钮负责打开仓库。
- GitHub 按钮旁有复制按钮，用于复制仓库 `.git` 下载地址。
- Star 标签已经修复为整行垂直居中；中文简介一行或两行时不应再偏上。
- 移动端使用两行卡片布局，桌面端使用表格式 Top 100 布局。

## 5. 前端关键文件

### 本轮发现体验改动（纳入 1.3.0 候选，以下为阶段记录）

- 截图边缘清理：`dsh-plugin-market.png` 与 `dsh-install-confirm.png` 去掉原截图弹窗四个圆角外残留的灰色遮罩，仅将外侧连通灰色像素补为白底。尺寸仍为 1600×1600 / 960×802，分别只改 2,189 / 598 个角落背景像素，其余像素逐一验证完全一致；未重绘文字或内部界面。两份 README 和官网继续复用同路径图片，PNG 合计约 305 KiB。55 项网页测试、差异空白检查通过，未提交或发布。
- GitHub 与 npm README 已整理为对外介绍：统一 `dsh-top100` 命名、官网/安装入口、功能概览与两条默认 npx 命令；GitHub 先展示官网榜单，npm 先展示插件，安装确认截图折叠展示，保留来源校验不等于安全审核的说明。新增 `web/public/assets/dsh-website-preview.jpg`（1280×1004，来自本地官网的真实浏览器截图），复用已有市场/确认 PNG；截图注明本地开发版，不覆盖旧封面文件。npm README 使用 GitHub `main` 的绝对图片 URL，图片不加入安装包；正式发布须先提交并推送这些资产到 `main`、确认链接可读，再发布新的 npm 版本以更新包页面。当前仅准备本地文档，尚未验证远端图片可访问，未提交、推送、升级版本或发布。验证：55 项网页测试（含 4 项 README/图片契约）、`git diff --check` 通过；`npm pack --dry-run --ignore-scripts --offline` 预检成功，58 个文件包含 README、无截图，压缩大小约 101 KiB。本次仅检查包清单，未重跑构建或真实安装，不代替正式发版验收。
- 安装指南按阅读顺序收整：前置环境要求与安装范围，主线仅保留「安装榜单插件 → 启动 DSH Web → 进入插件排行」；安装结果放在独立的「安装后的界面」，市场截图最大宽 640px，安装其他插件的说明及确认截图（最大宽 480px）默认折叠，最后才是其他方式与帮助。删除重复介绍、统一辅助标题为深色、缩小帮助字号，阅读栏最大宽 800px；为指南增加 64px 滚动留白，避免标题被吸顶导航遮住。两张图来自用户 2026-09-03 原截图，只裁切、不重绘，PNG 仍为 `web/public/assets/dsh-plugin-market.png`（1600×1600）和 `dsh-install-confirm.png`（960×802），合计约 382 KiB，保留开发版说明与原尺寸大图入口。801px / 390px 实测首屏标题完整、展开项及图片正常、无横向溢出；51 项网页测试及差异空白检查通过，六条安装/启动命令不变，未改插件逻辑、未提交或发布。本项取代此前两图并排的布局。
- 插件内移除自家 `#0` 置顶卡片：列表直接展示真实榜单条目，GitHub 图标移到页面顶部 `dsh-top100` 标题旁，保留 36px 点击区域、可访问名称、安全外链属性与键盘焦点。官网 `#000` 入口及共享展示规则保持不变；清理插件内已不用的出品卡片样式和文案，不改排序、数量、配色或安装流程。本地 801px 实测首项为 `#1`、数量仍为 `40 / 100`；700px 总榜标题与图标居中对齐、无横向溢出。209 项插件测试、49 项网页测试、类型检查及构建通过，预览已更新，未提交或发布。下方历史记录中插件内自家卡片的布局和编号以本项最终移除为准。
- 列表操作按钮收紧：文案简化为「安装」/「Install」，仍进入来源与风险确认。安装与查看项目统一高 36px、左右内边距 16px、最小宽 64px、内容自适应宽度及右边缘对齐；移除旧的固定 124px 和窄屏半行宽规则，保持参考图绿色与现有字体。801px / 700px 浏览器实测安装宽 64px、查看项目约 106px，右边缘一致且无横向溢出；208 项插件测试、类型检查及构建通过，已更新本地预览，未提交或发布。
- 按钮与状态字体收整：按用户参考截图取色，搜索和安装操作统一 14px / 600、固定青绿底 `#67a298`、纯白字，悬停 `#5f998f`；不再让背景通过 `currentColor` 混入文字色。用户明确选择还原原色，不采用加深方案；白字对比度约 2.92:1，仍有限，样式测试验证参考图还原而非对比度达标。来源状态移除胶囊底色、圆角与内边距，继承辅助信息的 12px / 400 字体，已识别保留绿色、未识别保留灰色，安装逻辑不变。207 项插件测试、类型检查及构建通过；801px 浏览器实测两个按钮底色均为 RGB(103, 162, 152)，与截图取色一致，已更新本地预览，未提交或发布。
- 最新细节统一：插件内置顶编号改为 `#0`（官网仍为 `#000`）；结果数量整行使用 13px / 400，不单独加粗；Stars 排序入口改名「总榜」，仍按 GitHub Stars 总数排序；安装弹窗的脚本竖线改用品牌绿，保留脚本原文、风险提示和显式同意门槛。203 项插件测试、类型检查、构建及差异空白检查通过；801px 实测列表字重、总榜切换、绿色脚本标记和勾选启停正常，未执行实际安装、未提交或发布。以下记录中的旧编号及脚本标记颜色以本项为准。
- 安装确认弹窗收紧为「安装项目名？」与精确安装源、脚本原文、一句重启提醒和确认区；移除重复简介/作者/类型/开场说明及多余标题，常规来源校验结果与技术字段放入「来源与校验详情」。来源身份存疑、Skill 主动内容等异常风险仍默认可见；生命周期提示仅在已展示脚本时去重，普通重启信息简写但不隐藏 warning 级提示。宽度 480px，dsh-market 默认高度约 401px；浅灰代码区、宿主文字色和克制的琥珀色风险标记，保留风险同意门槛及单一内容滚动区。8 项 UI/风险呈现测试、199 项插件测试、类型检查及构建通过；801px/390px 预览无横向溢出，详情展开、勾选启停实测正常，未执行实际安装。官网已同步「未识别安装源」，49 项网页测试在上一轮通过；安装判定未改、未发布。
- 插件市场视觉收整：保留 Harness 系统字体，操作按钮统一 14px / 600，简介 13px、辅助信息 12px；插件与 Skills 改成相邻的同级切换，数量使用千位分隔并移除胶囊。插件内 #000 仅保留带可访问名称、键盘焦点和 44px 点击区域的 GitHub 图标，移除重复安装指南；未识别来源的状态改为「未识别安装源」，中英文同步，不改变安装判定。类型检查、191 项插件测试及构建通过；1280px、801px 和 700px 视口无插件横向溢出，Skills 独立目录、来源筛选与 GitHub 键盘焦点实测正常。390px 下 Harness 固定侧栏将插件内容挤到约 98px，仍存在宿主级窄屏限制，本轮未改宿主、未发布。
- 安装指南对比度与展开层级补充：仅该页使用更深正文、深绿命令区和浅色命令文字；问题标题用深色，方式小标题用绿色。全局 CLI 与源码各自使用一个连续命令组，每条命令仍可独立复制；展开内容保留缩进，移除左侧竖线与方式间的分隔线。49 项网页测试通过（含分组与文字对比度检查），桌面与 390px 窄屏无横向溢出，复制与键盘折叠实测正常。其余榜单配色和安装命令不变，未上线。
- 安装指南精简为三步主线，保留两条默认命令和明确的成功判断；全局 CLI/源码方式、入口排查、npx 排查、版本与数据来源放入默认关闭的原生折叠项。三种启动方式共六条命令保持不变，不执行安装。移除重复标题、功能卡片和厚重步骤卡片，使用统一正文、清晰的等宽命令与复制按钮。验证：`npm run check` 类型检查及 349 项测试通过；本地预览在 801px/1280px 检查布局、复制、键盘折叠和往返导航。Docker daemon 未运行，未做容器验收或真实 DSH 安装，也未提交、发布。
- 官网与插件的热度榜、新锐榜、Stars 总榜都在同一列表内置顶展示 `#000 dsh-top100`，复用普通条目样式，标注「本站出品 · 不参与排名」，不写入数据集、不占榜单名额。搜索、分类、安装能力筛选时隐藏此入口，也不混入 Skills 技能库等其他目录；官网提供 GitHub 源码与安装指南入口，插件内只保留 GitHub 图标，不自动执行安装。
- 官网所有榜单的搜索都使用全量检索索引，默认按相关度排序并保留 Stars 总榜排名；无搜索时仍按各榜单原有口径展示。官网「仅看有安装源」可与分类、搜索叠加；总榜筛选读取完整索引，默认首屏仍只加载 manifest 与热度榜。
- 两端复用查询预编译与条目字段缓存，保留中文专业词（如「浏览器」「知识库」）以匹配中英文同义词。插件切换分类不清空正在使用的搜索词。
- 中文简介生成会拒绝通用模板、表格碎片和明显截断文本；缺少证据时不再从关键词猜测用途。既有坏简介由后续正常采集识别并重试，本轮不执行线上回填；仅更新前端不会改变公开快照中的旧简介。
- `npm run check` 更新网站共享模块并执行类型检查、单元测试；`npm run plugin:build` 更新仓库跟踪的插件构建产物。紧凑搜索索引新增可选 `needsConfig` 字段，旧快照仍兼容，字段缺失不宣称零配置。
- 安装筛选表示「有安装源」，不是安装成功率。未识别项目在官网与插件内统一标注「未识别安装源」，均保留项目来源入口。采集器、官网和插件共用 `plugin/src/shared/install-source.ts`：识别官方 npx 启动器、`--profile` 常见位置/等号写法、带引号的目标及标准 GitHub HTTPS 仓库地址；保留 ref，仍校验当前仓库/npm 包名。未知参数、其他域名、本地路径及 shell 组合命令仍不进入安装流程。
- README 解析支持行内命令、换行续写，保留安装章节之外的 DSH 命令，并优先保留它们；旧的前三条截断限制改为最多 32 条。解析版本更新后，下一次正常采集会重建旧检测缓存，可能增加该轮检测工作量。本地开发不运行线上采集或回填。Collector 镜像只复制该共享解析源文件，不依赖插件宿主代码。

```text
web/public/index.html       首页、榜单、多内容页切换、全部样式和前端逻辑
web/public/docs.html        最新 Docs 正文内容源及独立文档页面
web/public/dsh.html         接入 DSH 页面正文
web/public/dsheval.html     DSHeval 页面占位正文
web/public/data/            镜像构建时的静态数据目录
runtime/public-data/        Docker Web 容器实际只读挂载的公开榜单 JSON
docker/nginx.conf           静态站点与 /data 路由
docker/web.Dockerfile       Nginx Web 镜像
```

当前 `index.html` 是单文件静态应用，没有 React/Vue 构建链。修改时优先延续现有 HTML、CSS 和原生 JavaScript 结构，不要在一个小改动中擅自迁移框架。

## 6. 数据与数据库现状

当前使用 SQLite：

```text
runtime/dsh-top100.sqlite
```

当前本地状态：

- 数据库大小约 15 MB。
- `repositories`：3,414 条。
- `repository_daily_stats`：3,414 条。
- 最新快照日期：2026-08-21。
- `runtime/public-data/rankings.json` 生成时间：2026-08-21 17:27（北京时间）。
- Top 100：100 条。
- 新锐榜：100 条。
- 总榜：3,414 条。

主要表：

| 表 | 用途 |
| --- | --- |
| `repositories` | 仓库最新元数据、Stars、状态和发现来源 |
| `repository_daily_stats` | 每日 Stars、Forks 和 Issues 快照 |
| `repository_summaries` | 中文简介、来源哈希、模型和提示版本 |
| `collection_runs` | 采集执行状态、数量与错误审计 |

所有可迁移状态集中在 `runtime/`。迁移或备份时必须保持 SQLite 与公开 JSON 一致，优先使用 `./scripts/backup-runtime.sh`。

## 7. 数据更新链路

```text
GitHub / npm / Awesome / 社区线索
  → 候选发现
  → 确定性结构验证
  → 可选 DeepSeek 中文简介
  → SQLite 最新状态与每日快照
  → 排名计算
  → 原子发布 JSON
  → Nginx 静态前端
```

默认更新策略：

- 北京时间每天 06:00：增量发现并刷新已收录仓库。
- 每周日：完整分片发现。
- Scheduler 启动时默认只重新发布已有数据库快照，不立即访问 GitHub；`RUN_COLLECT_ON_STARTUP=true` 才会启动即抓取。

排名规则详见 `docs/ranking.md`。当前 Top 100 权重为：日增 35%、周增 25%、7 日增长率 15%、活跃度 10%、数据质量 10%、Stars 总量 5%。

## 8. 环境变量与密钥

配置模板位于 `.env.example`，本地真实密钥位于根目录 `.env`。不要读取、输出或提交 `.env`。

主要变量：

```text
GITHUB_TOKEN
DEEPSEEK_API_KEY
DEEPSEEK_API_BASE
DEEPSEEK_MODEL=deepseek-v4-flash
WEB_PORT=8080
COLLECT_HOUR=6
FULL_DISCOVERY_WEEKDAY=0
RUN_COLLECT_ON_STARTUP=false
DSH_DISCOVERY_MODE=incremental
```

前端永远不接触 GitHub Token、DeepSeek API Key 或 SQLite，只读取 `/data/*.json`。

## 9. 本地启动与验证

只做网页视觉与交互开发时，使用同源开发预览。它提供本地 `web/public`，并把
`/data/*` 按需代理到公开的 DSHeval 榜单接口；`/api/events` 在本地直接返回
`204`，不会写入线上分析事件：

```bash
npm run serve
```

默认地址为 `http://127.0.0.1:4173/`。可使用 `WEB_PORT` 修改端口，或用
`DSH_DATA_ORIGIN` 指定另一套兼容的公开数据服务。完全不需要榜单数据的静态检查
仍可运行 `npm run serve:static`。

只构建前端：

```bash
docker compose up -d --build web
```

启动前端和每日调度器：

```bash
docker compose up -d --build web scheduler
```

当前本地服务：

```text
Web        http://localhost:8080
Docs 视图 http://localhost:8080/?page=docs#docs
榜单视图 http://localhost:8080/#ranking
```

基础检查：

```bash
docker compose ps
curl --fail http://127.0.0.1:8080/
curl --fail http://127.0.0.1:8080/data/rankings.json
npm run typecheck
npm test
git diff --check
```

前端改动必须在真实 Docker Web 容器中验证，至少检查：

1. 首页首屏和桌面宽度布局。
2. Docs 点击后 URL 仍是 `/`，只增加 `?page=docs#docs`。
3. Docs 的“返回榜单”能够恢复 `/#ranking`。
4. Top 100、新锐榜、总榜切换正常。
5. 中文简介一行和两行时 Star 标签均处于榜单行垂直中心。
6. 搜索、隐藏 Skill 和复制 `.git` 地址正常。
7. 小屏幕没有横向溢出。

浏览器可能缓存单文件 HTML。若 Docker 重建后仍看到旧样式，可以先使用带临时查询参数的地址验证，例如 `http://localhost:8080/?dev=1#ranking`，但不要把该参数写入代码。

## 10. 常用命令

```bash
# 类型检查和测试
npm run check

# 使用现有配置执行一次采集并同步数据库
npm run update:once

# 手动 Docker 采集器
docker compose --profile manual run --rm collector

# 查看日志
docker compose logs --tail=200 web scheduler

# 备份 runtime
npm run runtime:backup

# 停止服务
docker compose down
```

运行采集会访问 GitHub，并可能调用 DeepSeek API。仅做前端开发时不要执行 `update:once` 或手动 collector。

## 11. 当前未完成与建议优先级

1. 根据 DSHeval 正式页面的后续变化维护外部入口与接入说明。
2. Docs 后续可补充单仓库排名解释、历史 Stars 曲线和项目提交入口。
3. 需要决定哪些 Logo 草稿保留；未得到用户确认前不要删除任何图片。
4. 当前多内容页前端改动尚未提交；提交前应先让用户确认最终页面。
5. 服务器版本与本地版本可能不同。任何上线、数据迁移、Caddy 或 SSL 操作都必须转到服务器专用任务，并在操作前重新检查线上状态。

## 12. 交接边界

- 本地开发任务使用 `/Users/cdy/Desktop/Work/CP/dsh-top100/repo`。
- 不要修改 `/Users/cdy/Desktop/Work/DSH/deepseek-harness` 来实现 dsh-top100 功能。
- 不要 SSH 到 `47.238.229.20`，除非用户在服务器专用任务中明确授权。
- 不要自动推送 GitHub；只有用户明确要求时才提交和推送。
- 不要把 `.env`、SQLite、`runtime/`、备份文件或 API Key 加入 Git。
